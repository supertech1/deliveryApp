import 'package:flutter/material.dart';

class Bars extends StatelessWidget {
  static final screen_id = "/bars";
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Bars"),
      ),
    );
  }
}
